import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'choose4me-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
